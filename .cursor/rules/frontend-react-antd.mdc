---
description: React frontend conventions, Ant Design, theme, api client, and routing
globs: frontend/src/**/*.ts,frontend/src/**/*.tsx
alwaysApply: false
---

# Frontend (React + Ant Design) 约定

- 使用 **函数组件** 与 Hooks；新页面在 **`App.tsx`** 的 Routes 中注册，并放在 `MainLayout` 或 `SystemLayout` 下相应路径。
- **API 调用**：全部通过 **`frontend/src/api/client.ts`** 的 `api()` 及各 `*Api` 对象；新增或变更后端接口时同步在此文件增加/修改方法，保持路径与类型与后端一致。
- **主题**：通过 **`theme/index.ts`** 的 `getThemeConfig(resolvedTheme)` 与根组件 **ConfigProvider** 注入；不在业务组件内写死 theme algorithm 或 token；主题偏好用 localStorage 持久化。
- **Ant Design**：使用 5.x 组件；根已配置 `locale={zhCN}`；列表、表单、弹窗需在深色/浅色两种主题下可读（依赖 ConfigProvider theme）。
