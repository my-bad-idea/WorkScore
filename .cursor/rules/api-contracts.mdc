---
description: API base path, error response shape, JWT, and permission rules (roles system_admin/department_admin/user, recorder, scorer)
globs: backend/**/*.ts,frontend/src/api/**/*.ts
alwaysApply: false
---

# API 契约

- **基础路径**：前端请求以 **`/api`** 为 base（如 `/api/work-records`）；后端控制器使用 `@Controller('api/...')`。
- **错误响应**：HTTP 4xx/5xx 时 body 为 JSON，至少包含 **`message: string`**；前端 `api/client.ts` 解析该字段并 throw Error(message)。
- **认证**：除安装状态、安装初始化、登录接口外，其余 API 需在 Header 中携带 **`Authorization: Bearer <token>`**；未提供或无效返回 401。
- **权限**（角色：system_admin | department_admin | user）：
  - **系统管理员**：部门 CRUD、岗位/人员/设置全部可写；考核队列、AI 考核测试可访问。
  - **部门管理员**：部门仅读；岗位/人员仅可写本部门（department_id === 当前用户 departmentId）；考核队列、AI 考核测试可访问；系统设置不可见/不可用。
  - **普通用户**：部门/岗位/人员仅读；考核队列、AI 考核测试、系统设置不可见/不可用。
  - **记录人**：工作记录的 PUT/DELETE 仅 `recorder_id === 当前用户 id`；否则 403。
  - **评分人**：删除评分记录（DELETE score-records/:id）仅 `scorer_id === 当前用户 id`；否则 403。
- **冲突与唯一性**：
  - 日报同人同日已存在、周报同人同周已存在：创建/更新工作记录返回 **400**，message 如「当日已有日报」「该周已有周报」。
  - 不能给自己的记录评分；同一人已对该工作记录评过分时再提交人工评分：返回 **409** 或 400，并明确提示。
